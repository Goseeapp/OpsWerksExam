"""User models - using Django's built-in User model."""
from django.contrib.auth.models import User

# Using Django's built-in User model
# Fields available: username, password, email, first_name, last_name
